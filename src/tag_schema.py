"""
tag_schema.py

Stub — not yet implemented.

Will define the verification tag schema: a structured record of what was
checked, by which method, and what the result was (pass/fail, match score,
matched text). A tag that records only bucket classification without the
actual check result is itself a non-discriminating check.
"""
